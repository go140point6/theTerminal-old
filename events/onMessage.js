async function onMessage(message) {
    //if(message.author.bot) return;
    
    //message.reply("Bingo")
    //console.log(message);

    const onMessage = message;
};

module.exports = { 
    onMessage
}